import styled from "styled-components/native";

export const ModalSelectTransactionsRangeContainer = styled.View`
    padding: 12px;
    background: ${({ theme }) => theme.colors.shape};
    border-radius: 8px;
`;

